// Content types for the Paid In Full clone

export interface InterviewCard {
  title: string;
  accent: string;
  videoId: string;
  thumbnailUrl: string;
}

export interface ProofCard {
  src: string;
  alt: string;
}
